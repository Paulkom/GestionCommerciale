{{ form_start(form) }}
<div class="row">
    <div class="fv-row mb-4 col">
        {{ form_label(form.typeOperation) }}
        {{ form_widget(form.typeOperation, {
            'attr':{
                'data-select-url': path('app_load_select', {'class':'TypeOperation'}),
                'data-select-columns':'id;libelle',
                'data-select-text':'libelle'
            }
        }) }}
    </div>
    <div class="fv-row mb-4 col">
        {{ form_label(form.client) }}
        {{ form_widget(form.client, {
            'attr':{
                'data-select-url': path('app_load_select', {'class':'Client'}),
                'data-select-columns':'id;nom;prenom;sigle',
                'data-select-text':'nom;prenom;sigle'
            }
        }) }}
    </div>
    <div class="fv-row mb-4 col">
        {{ form_label(form.modePaiement) }}
        {{ form_widget(form.modePaiement) }}
    </div>
</div>

{# <div class="row">
     <div class="fv-row mb-5 col">
        {{ form_label(form.compteBanque) }}
        {{ form_widget(form.compteBanque) }}
    </div> 
</div> #}

<div class="row">
    <div class="fv-row mb-5 col">
        {{ form_label(form.refTransac) }}
        {{ form_widget(form.refTransac) }}
    </div>
    <div class="fv-row mb-5 col">
        {{ form_label(form.montantTransac) }}
        {{ form_widget(form.montantTransac) }}
    </div>
</div>

<div class="separator mb-6"></div>
<button type="submit" class="btn btn-warning">
    <span class="indicator-label">Enregistrer</span>
    <span class="indicator-progress">Veuillez patienter...
            <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
        </span>
</button>
{{ form_end(form) }}
