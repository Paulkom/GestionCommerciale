<form method="post" action="{{ path('app_demande_de_prix_delete', {'id': demande_de_prix.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_de_prix.id) }}">
    <button class="btn">Delete</button>
</form>
