{% extends 'base.html.twig' %}

{% block title %}TypeOperation{% endblock %}

{% block body %}
    <h1>TypeOperation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_operation.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_operation.libelle }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_operation.createdAt ? type_operation.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_operation.updatedAt ? type_operation.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ type_operation.deletedAt ? type_operation.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_operation_index') }}">back to list</a>

    <a href="{{ path('app_type_operation_edit', {'id': type_operation.id}) }}">edit</a>

    {{ include('type_operation/_delete_form.html.twig') }}
{% endblock %}
