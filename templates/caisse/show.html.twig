{% extends 'base.html.twig' %}

{% block title %}Caisse{% endblock %}

{% block body %}
    <h1>Caisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caisse.id }}</td>
            </tr>
            <tr>
                <th>DateOuverture</th>
                <td>{{ caisse.dateOuverture ? caisse.dateOuverture|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateFermeture</th>
                <td>{{ caisse.dateFermeture ? caisse.dateFermeture|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FondRoulement</th>
                <td>{{ caisse.fondRoulement }}</td>
            </tr>
            <tr>
                <th>SoldeCloture</th>
                <td>{{ caisse.soldeCloture }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ caisse.createdAt ? caisse.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ caisse.updatedAt ? caisse.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ caisse.deletedAt ? caisse.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_caisse_index') }}">back to list</a>

    <a href="{{ path('app_caisse_edit', {'id': caisse.id}) }}">edit</a>

    {{ include('caisse/_delete_form.html.twig') }}
{% endblock %}
