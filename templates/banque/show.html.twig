{% extends 'base.html.twig' %}

{% block title %}Banque{% endblock %}

{% block body %}
    <h1>Banque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banque.id }}</td>
            </tr>
            <tr>
                <th>Denomination</th>
                <td>{{ banque.denomination }}</td>
            </tr>
            <tr>
                <th>Sigle</th>
                <td>{{ banque.sigle }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ banque.adresse }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ banque.createdAt ? banque.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ banque.updatedAt ? banque.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_banque_index') }}">back to list</a>

    <a href="{{ path('app_banque_edit', {'id': banque.id}) }}">edit</a>

    {{ include('banque/_delete_form.html.twig') }}
{% endblock %}
