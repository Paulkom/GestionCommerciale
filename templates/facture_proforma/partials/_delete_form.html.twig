<form method="post" action="{{ path('app_facture_proforma_delete', {'id': facture_proforma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_proforma.id) }}">
    <button class="btn">Delete</button>
</form>
