{{ form_start(form) }}
<div class="row">
    <div class="fv-row mb-5 col" id="trans">
        {{ form_label(form.magasinTrans2) }}
        {{ form_widget(form.magasinTrans2) }}
    </div>
</div>
{{ form_end(form) }}