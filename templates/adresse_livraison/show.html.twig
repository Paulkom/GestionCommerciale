{% extends 'base.html.twig' %}

{% block title %}AdresseLivraison{% endblock %}

{% block body %}
    <h1>AdresseLivraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse_livraison.id }}</td>
            </tr>
            <tr>
                <th>LibelleAdresse</th>
                <td>{{ adresse_livraison.libelleAdresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adresse_livraison_index') }}">back to list</a>

    <a href="{{ path('app_adresse_livraison_edit', {'id': adresse_livraison.id}) }}">edit</a>

    {{ include('adresse_livraison/_delete_form.html.twig') }}
{% endblock %}
