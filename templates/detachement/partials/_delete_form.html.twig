<form method="post" action="{{ path('app_detachement_delete', {'id': detachement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detachement.id) }}">
    <button class="btn">Delete</button>
</form>
