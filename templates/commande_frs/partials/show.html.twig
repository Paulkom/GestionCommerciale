{% extends 'base.html.twig' %}

{% block title %}CommandeFrs{% endblock %}

{% block body %}
    <h1>CommandeFrs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_fr.id }}</td>
            </tr>
            <tr>
                <th>DateComFrs</th>
                <td>{{ commande_fr.dateComFrs ? commande_fr.dateComFrs|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>MontantTotal</th>
                <td>{{ commande_fr.montantTotal }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande_fr.createdAt ? commande_fr.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_fr.updatedAt ? commande_fr.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ commande_fr.deletedAt ? commande_fr.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_frs_index') }}">back to list</a>

    <a href="{{ path('app_commande_frs_edit', {'id': commande_fr.id}) }}">edit</a>

    {{ include('commande_frs/_delete_form.html.twig') }}
{% endblock %}
