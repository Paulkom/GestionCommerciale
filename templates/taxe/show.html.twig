{% extends 'base.html.twig' %}

{% block title %}Taxe{% endblock %}

{% block body %}
    <h1>Taxe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taxe.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ taxe.libelle }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ taxe.valeur }}</td>
            </tr>
            <tr>
                <th>Appellation</th>
                <td>{{ taxe.appellation }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ taxe.createdAt ? taxe.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ taxe.updatedAt ? taxe.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ taxe.deletedAt ? taxe.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_taxe_index') }}">back to list</a>

    <a href="{{ path('app_taxe_edit', {'id': taxe.id}) }}">edit</a>

    {{ include('taxe/_delete_form.html.twig') }}
{% endblock %}
