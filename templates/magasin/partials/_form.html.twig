{{ form_start(form) }}
    <div>
        <div class="row">
            <div class="mb-5 fv-row fv-plugins-icon-container col-4">
                {{ form_widget(form.estPrincipal) }}
                {{ form_label(form.estPrincipal) }}
            </div>
        </div>
         <div class="row">
            <div class="mb-5 fv-row fv-plugins-icon-container col-3">
                {{ form_label(form.codeMag) }}
                {{ form_widget(form.codeMag) }}
            </div>
            <div class="mb-5 fv-row fv-plugins-icon-container col-5">
                {{ form_label(form.nomMag) }}
                {{ form_widget(form.nomMag) }}
            </div>
            <div class="mb-5 fv-row fv-plugins-icon-container col-4">
                {{ form_label(form.superviseur) }}
                {{ form_widget(form.superviseur) }}
            </div>
        </div>
        <div class="row">
            <div class="mb-5 fv-row fv-plugins-icon-container col-12">
                {{ form_label(form.adresse) }}
                {{ form_widget(form.adresse) }}
            </div>
        </div>
        <div class="mb-5 fv-row fv-plugins-icon-container float-end">
            <!--begin::Button-->
            <button type="reset" data-kt-cond-type="cancel" class="btn btn-light me-3">Vider</button>
            <!--end::Button-->
            <button type="submit" class="btn btn-warning">
                <span class="indicator-label">
                    Enregistrer
                </span>
                <span class="indicator-progress">
                   Veuillez patienter svp... <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                </span>
            </button>
        </div>
    </div>
{{ form_end(form) }} 

