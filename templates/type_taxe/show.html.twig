{% extends 'base.html.twig' %}

{% block title %}TypeTaxe{% endblock %}

{% block body %}
    <h1>TypeTaxe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_taxe.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ type_taxe.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_taxe.libelle }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_taxe.createdAt ? type_taxe.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_taxe.updatedAt ? type_taxe.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_taxe_index') }}">back to list</a>

    <a href="{{ path('app_type_taxe_edit', {'id': type_taxe.id}) }}">edit</a>

    {{ include('type_taxe/_delete_form.html.twig') }}
{% endblock %}
